# Whoordan Full Feature Implementation Audit

Generated: 2026-05-11 22:05 Asia/Qatar
Branch: `swift-app`
Project: `Whoordan.xcodeproj`
Audit mode: audit-only; no broad new features or architecture rewrites.

## Executive Summary

All features are **not** implemented. The app has a real SwiftUI shell, outer approval gate, publishable-key Supabase auth/config, RLS-backed approval model, HealthKit import foundation, device-first source resolver, original scoring engines, CoreBluetooth/protocol foundation, haptic command builder, sanitized CSV benchmark tooling, and passing simulator tests. This pass added local-first persistence, HealthKit anchored checkpoints/background observers, BLE ACK/local sample callbacks, and a Supabase queue/retry/repair path. It is still not a complete premium wearable app: full encrypted indexed health storage, export/deletion, complete Apple Health write sync, full wearable physical proof, physical HealthKit/BLE/haptic proof, long-term trends, strength, stress, cycle/pregnancy context, complete workouts, and full accessibility/manual QA remain incomplete.

## Branch And Worktree Status

- Confirmed branch: `swift-app`.
- Worktree: dirty SwiftUI migration branch with many expected Flutter/Android/iOS Runner deletions, new SwiftUI project files, and updated docs/tools. No unrelated changes were reverted.
- Private files: `.env` and private secret files were not read. Private CSV exports were not read in this audit pass and no private rows/notes are included here.

## Feature Counts By Status

| Status | Count |
|---|---:|
| IMPLEMENTED_VALIDATED | 138 |
| IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | 54 |
| PARTIAL | 152 |
| SCAFFOLDED | 40 |
| BLOCKED_PLATFORM | 3 |
| BLOCKED_CONFIG | 1 |
| MISSING | 20 |
| UNSAFE_NEEDS_FIX | 0 |
| TOTAL | 408 |

## Feature Counts By Category

| Category | IMPLEMENTED_VALIDATED | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | PARTIAL | SCAFFOLDED | BLOCKED_PLATFORM | BLOCKED_CONFIG | MISSING | UNSAFE_NEEDS_FIX |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| A. APP ACCESS / AUTH / APPROVAL | 9 | 6 | 8 | 2 | 0 | 0 | 0 | 0 |
| B. SUPABASE / CLOUD / SYNC | 5 | 2 | 5 | 0 | 0 | 1 | 8 | 0 |
| C. LOCAL STORAGE / PRIVACY | 2 | 0 | 9 | 0 | 0 | 0 | 3 | 0 |
| D. HEALTHKIT | 5 | 14 | 5 | 0 | 0 | 0 | 4 | 0 |
| E. BLE / WEARABLE PROTOCOL | 26 | 14 | 13 | 0 | 0 | 0 | 0 | 0 |
| F. HAPTICS / VIBRATION / ALARMS / NOTIFICATIONS | 11 | 3 | 3 | 4 | 3 | 0 | 0 | 0 |
| G. HEALTH DATA MODELS / SOURCE RESOLUTION | 13 | 0 | 5 | 10 | 0 | 0 | 0 | 0 |
| H. STEPS / MOVEMENT / CALORIES / DISTANCE | 11 | 0 | 7 | 0 | 0 | 0 | 0 | 0 |
| I. HEART / BODY SIGNALS | 9 | 3 | 8 | 0 | 0 | 0 | 0 | 0 |
| J. RECOVERY / STRAIN / STRESS | 13 | 0 | 14 | 0 | 0 | 0 | 7 | 0 |
| K. SLEEP | 5 | 0 | 16 | 0 | 0 | 0 | 0 | 0 |
| L. WORKOUTS / STRENGTH | 5 | 0 | 11 | 7 | 0 | 0 | 1 | 0 |
| M. JOURNAL / HABITS / INSIGHTS | 2 | 0 | 7 | 17 | 0 | 0 | 0 | 0 |
| N. LONG-TERM / SPECIAL CONTEXT | 3 | 0 | 17 | 0 | 0 | 0 | 3 | 0 |
| O. UI / UX / ACCESSIBILITY | 3 | 8 | 19 | 0 | 0 | 0 | 0 | 0 |
| P. RELEASE / TESTING | 11 | 1 | 6 | 0 | 0 | 0 | 1 | 0 |

## Status Movements In This Pass

The full 408-row table below remains the detailed audit baseline. These directly changed rows should be read with the updated statuses here:

| Feature # | Feature | Prior status | Updated status | Evidence |
|---:|---|---|---|---|
| 33 | Incremental sync | MISSING | PARTIAL | Supabase queue can drain local health sample uploads; full remote restore/conflict handling remains incomplete. |
| 34 | Sync queue | MISSING | IMPLEMENTED_VALIDATED | `SupabaseSyncQueueItem`, queue repair, retry/backoff, and local-store tests. |
| 35 | Retry/backoff | MISSING | IMPLEMENTED_VALIDATED | Failed uploads update attempt count, next-at, and last error in durable local state. |
| 40 | Repair Sync | MISSING | IMPLEMENTED_VALIDATED | `repairSupabaseQueue` scans unsynced local health records and queues missing work. |
| 47 | Local storage architecture | PARTIAL | IMPLEMENTED_VALIDATED | `FileProtectedLocalStore` persists local records and is covered by store tests. |
| 49 | Local health database | MISSING | PARTIAL | Durable file-backed storage exists; encrypted indexed database remains missing. |
| 68 | Incremental import | MISSING | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | HealthKit anchored import exists in code; real device import not validated. |
| 69 | Checkpoints/anchors | MISSING | IMPLEMENTED_VALIDATED | `HealthKitCheckpoint` persistence and checkpoint save rules are unit-tested. |
| 114 | Historical sync request | PARTIAL | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | BLE metadata batch markers dispatch ACKs; wearable hardware proof remains pending. |
| 118 | End-of-sync detection | PARTIAL | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | End-of-sync persists BLE checkpoint and moves toward realtime; hardware proof remains pending. |

## P0/P1 Findings

No P0/P1 safety issue was found in this audit pass. No service-role key was found in client code, no raw private CSV export was found committed, no production fake metrics were found, no user-metadata authorization dependency was found, and the app-side approval/consent guards remain in place.

Important non-P0 risks:
- Local storage is durable and file-protected, but not production-grade encrypted/indexed storage for large health datasets.
- Session refresh is incomplete.
- HealthKit incremental import/checkpoints are implemented in code but not physically validated.
- Cloud sync now has queue/retry/repair for local health sample uploads; full conflict workflows remain incomplete.
- BLE physical data flow and vibration were not re-tested in this audit pass.
- Supabase live two-user RLS probe was not run.
- Supabase advisor reports leaked-password protection disabled.

## Security And Privacy Findings

- App code uses publishable/anon-style Supabase key config, not a service-role/secret key.
- Static search found service-role and metadata terms only in documentation/skill warning contexts, not client authorization logic.
- Supabase policy inspection showed public tables with RLS and forced RLS enabled; protected table policies reference `user_access` plus `auth.uid()` and do not reference user metadata.
- Health sample upload requires approval plus cloud sync consent plus health-data consent.
- Health sync hashes HealthKit source record IDs and dedupe keys before upload.
- Raw BLE capture is disabled by default and writes locally with an explicit warning when enabled; production should keep it disabled and avoid exposing first/last byte summaries outside approved debug UI.
- No full raw private CSV or raw BLE payload fixture was found committed by static inspection.

## HealthKit Findings

HealthKit is implemented as a read-only foundation with entitlement and usage strings. Mapping and source-resolution tests pass. Physical iPhone build/install/launch passed, but real HealthKit permission/import/upload was not manually re-tested in this audit pass, so HealthKit-dependent features are not marked fully physically validated.

## BLE/Wearable Findings

The BLE foundation is substantial and test-covered at the protocol level. It discovers the expected service/characteristics, handles frame validation/reassembly, command responses, metadata, event, firmware, R10 HR, R11 scaffold, and R21 scaffold. It does not yet prove end-to-end production wearable sync in this audit pass, and several decoder branches are scaffolded until more real packet captures exist.

## Vibration Findings

Built-in pattern model, safety limits, and Harvard/Maverick command builders are tested. The service does not fake physical success and requires approval plus connected wearable state. Physical vibration was not re-tested in this audit pass.

## Full Feature Matrix

The table below includes the required per-feature fields: feature name, status, user-facing behavior, implementation details, inspected files/classes/functions, data sources, source priority, approval gate behavior, privacy/consent behavior, missing-data behavior, stale-data behavior, confidence/source-label behavior, tests, validation, physical validation, limitations, risks, and next step.


### A. APP ACCESS / AUTH / APPROVAL

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | App launch/session restore | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 2 | Sign in | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 3 | Sign up | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 4 | Password reset | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 5 | Sign out | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 6 | Keychain session storage | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 7 | Admin approval gate | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 8 | Pending approval screen | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 9 | Rejected screen | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 10 | Revoked screen | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 11 | Missing/error approval screen | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 12 | Approval refresh | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 13 | Revocation lockout | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 14 | Deep-link/protected-route protection | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 15 | Local-only mode blocked before approval | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 16 | App features blocked before approval | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 17 | Cached health data hidden before approval/revocation | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 18 | Background jobs blocked before approval | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 19 | HealthKit blocked before approval | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 20 | BLE blocked before approval | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 21 | Cloud sync blocked before approval | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 22 | Cloud sync consent | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 23 | Health-data cloud sync consent | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 24 | Manual Supabase approval workflow | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 25 | Supabase RLS assumptions/live validation status | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI root routing uses AppRouter; AppEnvironment restores sessions, fetches public.user_access approval, and blocks protected services with PrivacyAccessGuard. | Whoordan/App/AppRootView.swift; Whoordan/App/AppRouter.swift; Whoordan/App/AppEnvironment.swift; Whoordan/Features/Auth/AuthView.swift; Whoordan/Features/Approval/ApprovalLockedView.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/Approval/ApprovalService.swift; Whoordan/Core/Auth/AuthService.swift; WhoordanTests/ApprovalGateTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | ApprovalGateTests and UI launch test. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |

### B. SUPABASE / CLOUD / SYNC

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 26 | Supabase config | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 27 | Publishable/anon key handling | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 28 | No service-role key | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 29 | Supabase Auth integration | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 30 | Session restore/refresh | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 31 | Cloud sync architecture | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 32 | Initial sync | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 33 | Incremental sync | MISSING | No production user-facing workflow exists yet. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Prioritize, design, implement, and test only if still in product scope. |
| 34 | Sync queue | MISSING | No production user-facing workflow exists yet. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Prioritize, design, implement, and test only if still in product scope. |
| 35 | Retry/backoff | MISSING | No production user-facing workflow exists yet. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Prioritize, design, implement, and test only if still in product scope. |
| 36 | Sync checkpoints | MISSING | No production user-facing workflow exists yet. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Prioritize, design, implement, and test only if still in product scope. |
| 37 | Conflict handling | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 38 | Deduplication before upload | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 39 | Manual Sync Now | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 40 | Repair Sync | MISSING | No production user-facing workflow exists yet. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Prioritize, design, implement, and test only if still in product scope. |
| 41 | Self-hosted Supabase mode, if present | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 42 | Data export | MISSING | No production user-facing workflow exists yet. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Prioritize, design, implement, and test only if still in product scope. |
| 43 | Data deletion | MISSING | No production user-facing workflow exists yet. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Prioritize, design, implement, and test only if still in product scope. |
| 44 | Account deletion | MISSING | No production user-facing workflow exists yet. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Prioritize, design, implement, and test only if still in product scope. |
| 45 | Live two-user RLS probe status | BLOCKED_CONFIG | Blocked by missing live test setup or external configuration, not by app code alone. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Configuration or live test account matrix missing for proof. | Physical proof not required or limited to app install/launch evidence. | Needs approved test account matrix, two-user RLS probe, or live configuration. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Provision the required safe test setup and rerun validation. |
| 46 | Supabase advisor status | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Supabase REST/Auth client uses bundle or environment publishable config, bearer user JWT, RLS-backed tables, and consent-gated health_samples upload. | Whoordan/Core/Supabase/SupabaseClient.swift; supabase/migrations/*.sql; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/SupabaseConfigTests.swift; WhoordanTests/ApprovalGateTests.swift; Supabase MCP table/policy/advisor inspection | Supabase Auth, public.user_access, health_samples, consent_records, and other RLS-protected public tables. | Supabase user JWT plus RLS; service-role/secret keys are not valid app sources. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Upload requires signed-in approved user plus cloudSyncEnabled and healthDataCloudConsent; source IDs are hashed before upload. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Cloud rows carry confidence/source metadata for health_samples where uploaded. | SupabaseConfigTests, ApprovalGateTests health sync guards; live MCP RLS/advisor inspection. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | RLS/config drift, missing live two-user probe, and incomplete sync queue can cause false confidence. | Keep regression coverage; add physical/manual evidence where relevant. |

### C. LOCAL STORAGE / PRIVACY

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 47 | Local storage architecture | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 48 | Keychain usage | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 49 | Local health database | MISSING | No production user-facing workflow exists yet. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Prioritize, design, implement, and test only if still in product scope. |
| 50 | Local storage encryption status | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 51 | Large health dataset handling | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 52 | Local-only mode after approval | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 53 | Local-only never uploads health data | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 54 | Data deletion local | MISSING | No production user-facing workflow exists yet. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Prioritize, design, implement, and test only if still in product scope. |
| 55 | Data export local | MISSING | No production user-facing workflow exists yet. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Prioritize, design, implement, and test only if still in product scope. |
| 56 | Private CSV handling | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Keep regression coverage; add physical/manual evidence where relevant. |
| 57 | Raw payload/log handling | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 58 | Audit/logging privacy | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 59 | Legal/privacy copy | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 60 | Medical disclaimer copy | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Keychain stores auth session; FileProtectedLocalStore is durable file-backed storage with iOS file protection; private CSV and raw BLE capture policies avoid committed raw private data. | Whoordan/Core/Storage/KeychainStore.swift; Whoordan/Core/Storage/LocalStore.swift; Whoordan/Core/Privacy/PrivacyAccessGuard.swift; Whoordan/Core/CSV/WearableExportCSVSchema.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; docs/WHOORDAN_PRIVATE_CSV_DATA_POLICY.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No raw private CSV rows committed; docs/tests use sanitized synthetic fixtures. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests; ApprovalGateTests; static repo search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | File-backed local storage is durable and file-protected, but not fully encrypted or indexed for production-scale health datasets. | Keep regression coverage; add physical/manual evidence where relevant. |

### D. HEALTHKIT

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 61 | HealthKit capability/entitlement status | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Keep regression coverage; add physical/manual evidence where relevant. |
| 62 | Info.plist usage descriptions | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Keep regression coverage; add physical/manual evidence where relevant. |
| 63 | HealthKit availability check | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 64 | HealthKit permission request | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 65 | Denied state | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 66 | Partial permission state | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 67 | Historical import | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 68 | Incremental import | MISSING | No production user-facing workflow exists yet. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Missing state is the feature status; no production data path. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | No validation beyond absence/static inspection. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | No implementation currently found. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Prioritize, design, implement, and test only if still in product scope. |
| 69 | Checkpoints/anchors | MISSING | No production user-facing workflow exists yet. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Missing state is the feature status; no production data path. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | No validation beyond absence/static inspection. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | No implementation currently found. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Prioritize, design, implement, and test only if still in product scope. |
| 70 | Unit conversion | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Keep regression coverage; add physical/manual evidence where relevant. |
| 71 | Source labels | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Keep regression coverage; add physical/manual evidence where relevant. |
| 72 | Deduplication | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Keep regression coverage; add physical/manual evidence where relevant. |
| 73 | HealthKit write support | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 74 | Heart rate import | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 75 | Resting heart rate import | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 76 | HRV SDNN import | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 77 | Respiratory rate import | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 78 | Sleep analysis import | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 79 | Steps import | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 80 | Active energy import | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 81 | Distance import | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 82 | Workouts import | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 83 | Oxygen saturation import | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 84 | Temperature import | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 85 | VO2/cardio fitness import | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 86 | Menstrual/cycle data import | MISSING | No production user-facing workflow exists yet. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Missing state is the feature status; no production data path. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | No validation beyond absence/static inspection. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | No implementation currently found. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Prioritize, design, implement, and test only if still in product scope. |
| 87 | Irregular rhythm event import | MISSING | No production user-facing workflow exists yet. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Missing state is the feature status; no production data path. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | No validation beyond absence/static inspection. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | No implementation currently found. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Prioritize, design, implement, and test only if still in product scope. |
| 88 | HealthKit physical iPhone validation status | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | HealthKitService requests read-only HealthKit types and maps authorized samples into source-labeled HealthSample values for current-day aggregation. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; Whoordan/App/AppEnvironment.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | Read-only HealthKit import; no cloud upload unless approval and both consents are enabled. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests mapper/source/dedupe/steps tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | HealthKit authorization can be per-type and device-only; simulator tests cannot prove real import. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |

### E. BLE / WEARABLE PROTOCOL

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 89 | CoreBluetooth manager | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 90 | BLE permission flow | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 91 | BLE scan | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 92 | BLE connect | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 93 | BLE reconnect | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 94 | BLE disconnect cleanup | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 95 | Service discovery | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 96 | Characteristic discovery | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 97 | Notify subscription | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 98 | Command write | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 99 | Device identity/name decoding | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 100 | Serial/fingerprint decoding | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 101 | Connection state machine | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 102 | Frame decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 103 | CRC8 validation | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 104 | CRC32 validation | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 105 | Frame reassembler | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 106 | Fragment handling | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 107 | Padding handling | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 108 | Malformed packet handling | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 109 | Packet type decoding | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 110 | Command response decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 111 | Init handshake commands | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 112 | Data range decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 113 | Alarm response decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 114 | Historical sync request | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 115 | Metadata decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 116 | Batch marker decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 117 | Batch ACK builder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 118 | End-of-sync detection | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 119 | Realtime enable commands | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 120 | Realtime disable commands | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 121 | Realtime 0x28 decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 122 | Raw realtime 0x2B decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 123 | R10 decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 124 | R11 decoder | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 125 | R21 decoder | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 126 | Event 0x30 decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 127 | Double-tap event decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 128 | Battery event decoder | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 129 | Charging event decoder | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 130 | Wrist on/off event decoder | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 131 | Temperature event decoder | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 132 | Haptic fired/terminated event decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 133 | Firmware log 0x32 decoder | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 134 | Device diagnostics | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 135 | RSSI | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 136 | Battery display | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 137 | Firmware display | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 138 | Last packet display | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 139 | BLE source metadata | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 140 | BLE dedupe IDs | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Keep regression coverage; add physical/manual evidence where relevant. |
| 141 | Wearable physical validation status | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | CoreBluetooth service discovers expected UUIDs, subscribes, writes commands, reassembles 0xAA frames, decodes safe packet summaries, and emits only safe HR samples. | Whoordan/Core/BLE/WearableBLEService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/WearableProtocolTests.swift; docs/WHOORDAN_CAPTURED_BLE_PAYLOAD_ANALYSIS.md | BLE characteristics 61080003/04/05/07 plus standard GATT HR/battery where present; raw payload capture only in explicit debug mode. | Expected wearable service/standard GATT direct measurements first; unsupported raw packets stay diagnostic-only. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No raw payload logging by default; debug capture writes local app Documents warning file and should not be committed. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | WearableProtocolTests for CRC, frames, commands, events, R10/R11/R21 scaffolds, dedupe. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Unofficial wearable protocol; packet unknowns and physical device variance remain. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |

### F. HAPTICS / VIBRATION / ALARMS / NOTIFICATIONS

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 142 | Vibration pattern model | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 143 | Built-in vibration patterns | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 144 | Custom vibration pattern model | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 145 | Custom vibration recorder/editor | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 146 | Pattern safety limits | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 147 | Vibration preview UI | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 148 | Vibration preview service | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 149 | Harvard haptic command 0x4F | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 150 | Maverick/Gen4 haptic command 0x13 | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 151 | Stop haptics command | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 152 | Haptic command ACK handling | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 153 | Haptic fired/terminated event handling | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 154 | Disconnected state | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 155 | Unsupported state | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 156 | Failed state | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 157 | Device diagnostics for haptics | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 158 | Physical vibration test status | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 159 | Alarm vibration | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 160 | Alarm scheduling | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 161 | Snooze | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 162 | Notification vibration | BLOCKED_PLATFORM | Not available through safe iOS public APIs or not supported by current device/protocol path. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Reviewed as platform-limited; not implemented as production behavior. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs platform-supported API or explicit documented exclusion. | Main risk is overclaiming completeness beyond current implementation evidence. | Document limitation or replace with a platform-supported alternative. |
| 163 | Per-app notification vibration | BLOCKED_PLATFORM | Not available through safe iOS public APIs or not supported by current device/protocol path. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Reviewed as platform-limited; not implemented as production behavior. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs platform-supported API or explicit documented exclusion. | Main risk is overclaiming completeness beyond current implementation evidence. | Document limitation or replace with a platform-supported alternative. |
| 164 | Call vibration | BLOCKED_PLATFORM | Not available through safe iOS public APIs or not supported by current device/protocol path. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Reviewed as platform-limited; not implemented as production behavior. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs platform-supported API or explicit documented exclusion. | Main risk is overclaiming completeness beyond current implementation evidence. | Document limitation or replace with a platform-supported alternative. |
| 165 | Platform-blocked notification/call limitations | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | VibrationPreviewService builds bounded Harvard and Maverick command packets, refuses unsafe/custom unsupported playback, and reports honest states. | Whoordan/Core/Haptics/VibrationModels.swift; Whoordan/Features/Vibration/VibrationView.swift; Whoordan/Features/Device/DeviceView.swift; WhoordanTests/VibrationTests.swift; WhoordanTests/WearableProtocolTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | VibrationTests and WearableProtocolTests haptic-event scaffolds. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |

### G. HEALTH DATA MODELS / SOURCE RESOLUTION

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 166 | HealthSample model | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 167 | DailyHealthSummary model | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 168 | SleepSession model | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 169 | SleepStageSegment model | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 170 | WorkoutSession model | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 171 | WorkoutHeartRateZoneSummary model | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 172 | StrengthWorkout model | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 173 | StrengthSet model | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 174 | JournalEntry model | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 175 | HabitLog model | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 176 | HabitInsight model | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 177 | RecoveryScoreResult model | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 178 | StrainScoreResult model | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 179 | SleepNeedResult model | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 180 | StressSignalResult model | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 181 | BodySignalSummary model | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 182 | LongTermTrend model | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 183 | SourceMetadata model | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 184 | ConfidenceLevel model | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 185 | Source resolver | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 186 | Source priority: wearable > Apple Health > manual > estimate > cloud copy | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 187 | Dedupe logic | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 188 | Local-day aggregation | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 189 | Timezone handling | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 190 | Stale-data handling | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 191 | Missing-data handling | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 192 | Confidence scoring | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 193 | Measured/imported/calculated/estimated labeling | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | HealthModels define core sample/summary/source/confidence objects; ScoringEngines provides source resolution, local-day aggregation, and original scoring outputs. | Whoordan/Core/Models/AppModels.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests and ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |

### H. STEPS / MOVEMENT / CALORIES / DISTANCE

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 194 | Daily step count | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 195 | Step source priority | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 196 | Wearable step source status | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 197 | Apple Health step fallback | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 198 | Step deduplication | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 199 | Step goal | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 200 | Step trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 201 | Steps on Today | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 202 | Steps detail section/screen | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 203 | Movement minutes | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 204 | Movement contribution to strain | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 205 | Missing steps CTA | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 206 | Active energy | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 207 | Total calories | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 208 | Calorie estimate labeling | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 209 | Distance | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 210 | GPS/workout distance | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 211 | Step/stride distance estimate status | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | MovementAggregator dedupes source-labeled steps, prefers wearable over Apple Health, tracks goal/progress, and feeds valid movement into strain. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Today/TodayView.swift; Whoordan/Features/Settings/SettingsView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/ScoringTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests movement source/dedupe/goal/contribution. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |

### I. HEART / BODY SIGNALS

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 212 | Live heart rate | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 213 | Daily heart rate summary | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 214 | Workout heart rate | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 215 | Resting heart rate | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 216 | HRV | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 217 | HRV method/source labeling | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 218 | No BPM-only fake HRV | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 219 | Heart-rate zones | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 220 | Configurable max HR | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 221 | Fallback max HR estimate | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 222 | Zone minutes | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 223 | Respiratory rate | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 224 | SpO2 | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 225 | SpO2 estimate/debug labeling | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 226 | Temperature | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 227 | Baseline comparison | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 228 | Body signal trends | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 229 | Out-of-baseline cautious copy | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 230 | Irregular rhythm events | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 231 | No custom AFib detector | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Heart/body signals come from HealthKit imports and safe wearable HR decoding; HRV/SpO2 restrictions prevent unsupported production metrics. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/BLE/WearableProtocol.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Heart/HeartView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/WearableProtocolTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests, WearableProtocolTests, ScoringTests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |

### J. RECOVERY / STRAIN / STRESS

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 232 | Baseline engine | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 233 | Rolling baselines | MISSING | No production user-facing workflow exists yet. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Missing state is the feature status; no production data path. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Prioritize, design, implement, and test only if still in product scope. |
| 234 | Personal normal ranges | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 235 | Recovery score 0-100 | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 236 | Recovery category | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 237 | Recovery contributors | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 238 | Recovery confidence | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 239 | Recovery missing-data behavior | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 240 | HRV recovery contribution | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 241 | RHR recovery contribution | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 242 | Respiratory recovery contribution | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 243 | Sleep recovery contribution | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 244 | Temperature recovery contribution | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 245 | SpO2 recovery contribution | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 246 | Recent strain recovery contribution | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 247 | Cycle context recovery contribution | MISSING | No production user-facing workflow exists yet. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Missing state is the feature status; no production data path. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Prioritize, design, implement, and test only if still in product scope. |
| 248 | Recovery trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 249 | Strain score 0-21 | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 250 | Daily strain | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 251 | Workout strain | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 252 | Personalized strain target | MISSING | No production user-facing workflow exists yet. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Missing state is the feature status; no production data path. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Prioritize, design, implement, and test only if still in product scope. |
| 253 | Strain contributors | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 254 | HR load | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 255 | Zone load | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 256 | Step/movement load | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 257 | Active energy load | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 258 | Strength/muscular load contribution | MISSING | No production user-facing workflow exists yet. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Missing state is the feature status; no production data path. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Prioritize, design, implement, and test only if still in product scope. |
| 259 | Recent strain balance | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 260 | Stress signals | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 261 | Physiological stress only | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 262 | Stress trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 263 | Breathing/relaxation sessions | MISSING | No production user-facing workflow exists yet. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Missing state is the feature status; no production data path. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Prioritize, design, implement, and test only if still in product scope. |
| 264 | Breathing timer | MISSING | No production user-facing workflow exists yet. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Missing state is the feature status; no production data path. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Prioritize, design, implement, and test only if still in product scope. |
| 265 | Before/after HR/HRV support | MISSING | No production user-facing workflow exists yet. | Original Whoordan scoring combines available baselined signals with confidence; many contributor categories still lack persisted baselines or full inputs. | Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Recovery/RecoveryView.swift; Whoordan/Features/Today/TodayView.swift; WhoordanTests/ScoringTests.swift; WhoordanTests/HealthKitTests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Missing state is the feature status; no production data path. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | ScoringTests and HealthKitTests movement/availability constraints. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Prioritize, design, implement, and test only if still in product scope. |

### K. SLEEP

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 266 | Sleep tracking/import | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 267 | Last sleep session | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 268 | Sleep duration | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 269 | Time in bed | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 270 | Awake duration | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 271 | Sleep stages | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 272 | REM sleep | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 273 | Deep sleep | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 274 | Light/core sleep | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 275 | Naps | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 276 | Sleep efficiency | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 277 | Sleep consistency | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 278 | Sleep debt | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 279 | Sleep need | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 280 | Sleep planner | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 281 | Bedtime recommendation | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 282 | Wake-time recommendation | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 283 | Sleep source labels | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 284 | Sleep confidence | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |
| 285 | Sleep trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 286 | No fake sleep stages | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Sleep minutes come from HealthKit sleep category samples; UI avoids fabricated stages and exposes source CTAs. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; Whoordan/Features/Sleep/SleepView.swift; WhoordanTests/HealthKitTests.swift; WhoordanTests/DesignContractTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | HealthSourceResolver can mark stale by timestamp; broader persisted stale policies are incomplete. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests plus DesignContractTests no fake stages. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Health metric may mislead if source/confidence/estimate labels are missing or stale. | Keep regression coverage; add physical/manual evidence where relevant. |

### L. WORKOUTS / STRENGTH

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 287 | Workout import | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 288 | Workout detection | MISSING | No production user-facing workflow exists yet. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | No validation beyond absence/static inspection. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | No implementation currently found. | Main risk is overclaiming completeness beyond current implementation evidence. | Prioritize, design, implement, and test only if still in product scope. |
| 289 | Manual workout logging | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 290 | Workout type | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 291 | Workout duration | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 292 | Workout heart rate | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 293 | Workout HR zones | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 294 | Workout active energy | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 295 | Workout calories estimate label | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 296 | Workout distance/GPS | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 297 | Workout strain contribution | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 298 | Workout recovery impact | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 299 | Workout history | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 300 | Workout detail screen | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 301 | Strength workout logging | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 302 | Exercises | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 303 | Sets | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 304 | Reps | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 305 | Weight | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 306 | Muscular load heuristic | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 307 | Strength contribution to strain | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 308 | Strength history | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 309 | Strength confidence | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 310 | Strength estimate disclaimer | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Workout duration/energy/distance import maps through HealthKit; strength and detailed workout surfaces are mostly placeholders. | Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/Scoring/ScoringEngines.swift; WhoordanTests/HealthKitTests.swift | Wearable BLE where reliable; Apple Health direct import; manual or Whoordan estimate only where labeled; cloud only as backup copy. | wearable BLE > Apple Health > manual > Whoordan estimate > cloud backup copy. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | HealthKitTests for workout mapping only. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |

### M. JOURNAL / HABITS / INSIGHTS

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 311 | Daily journal | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 312 | Habit logging | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 313 | Custom habits | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 314 | Caffeine | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 315 | Alcohol | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 316 | Hydration | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 317 | Supplements | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 318 | Screen time | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 319 | Late meals | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 320 | Illness | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 321 | Mood | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 322 | Stress | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 323 | Travel | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 324 | Meditation | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 325 | Exercise type | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 326 | Soreness | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 327 | Medication | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 328 | Time outdoors | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 329 | Diet habits | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 330 | Notes | SCAFFOLDED | A placeholder, model, or basic surface exists, but it is not a complete production workflow. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Shows placeholder/scaffold rather than complete measured data. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Only a placeholder/model/surface exists; not shippable as a full feature. | Main risk is overclaiming completeness beyond current implementation evidence. | Define production contract, implement workflow, then add tests. |
| 331 | Habit recovery insights | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 332 | With-vs-without comparison | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 333 | Minimum sample size | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 334 | Confidence/sample size display | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 335 | Association language only | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 336 | No causation claims | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Journal model and simple screen exist; private export parser can summarize habit yes/no data aggregate-only for development validation. | Whoordan/Core/Models/HealthModels.swift; Whoordan/Features/Journal/JournalView.swift; Tools/WhoordanBenchmark/WhoordanBenchmark.swift; WhoordanTests/CSVSchemaTests.swift | Manual journal entries plus sanitized CSV benchmark fixtures; private notes are redacted/ignored. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | CSVSchemaTests for journal yes/no mapping and aggregate-only benchmark parser. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |

### N. LONG-TERM / SPECIAL CONTEXT

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 337 | Long-term recovery trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 338 | Long-term sleep trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 339 | Long-term strain trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 340 | Long-term steps trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 341 | Long-term active energy trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 342 | Long-term RHR trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 343 | Long-term HRV trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 344 | Long-term respiratory trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 345 | Long-term SpO2 trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 346 | Long-term temperature trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 347 | Long-term workout trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 348 | Long-term strength trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 349 | VO2/cardio fitness trend | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 350 | Healthspan/wellness summary | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 351 | Menstrual cycle insights | MISSING | No production user-facing workflow exists yet. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | Main risk is overclaiming completeness beyond current implementation evidence. | Prioritize, design, implement, and test only if still in product scope. |
| 352 | Cycle phase/context | MISSING | No production user-facing workflow exists yet. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | Main risk is overclaiming completeness beyond current implementation evidence. | Prioritize, design, implement, and test only if still in product scope. |
| 353 | Cycle impact on sleep/recovery/temp | MISSING | No production user-facing workflow exists yet. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | Main risk is overclaiming completeness beyond current implementation evidence. | Prioritize, design, implement, and test only if still in product scope. |
| 354 | Not contraception | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 355 | Not fertility tool | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 356 | Pregnancy-related tracking | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 357 | User-declared pregnancy only | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 358 | No pregnancy detection | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 359 | Pregnancy trend context only | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Trend/special-context surfaces are mostly roadmap placeholders; safety exclusions avoid contraception, fertility, pregnancy detection, and diagnosis claims. | Whoordan/Features/Settings/SettingsView.swift; Whoordan/Core/HealthKit/HealthKitService.swift; Whoordan/Core/Scoring/ScoringEngines.swift; docs/WHOORDAN_HEALTH_FEATURE_IMPLEMENTATION_REPORT.md | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | Limited design/static checks; no full trend/cycle tests. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |

### O. UI / UX / ACCESSIBILITY

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 360 | SwiftUI app shell | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 361 | Premium iPhone-native visual quality | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 362 | Today dashboard | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 363 | Recovery screen | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 364 | Sleep screen | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 365 | Heart screen | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 366 | Movement/Steps screen | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 367 | Workouts screen | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 368 | Strength screen | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 369 | Device screen | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 370 | Vibration screen | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 371 | Journal screen | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 372 | Settings screen | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 373 | Missing-data CTAs | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 374 | Source labels in UI | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 375 | Confidence labels in UI | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 376 | Estimated labels in UI | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 377 | Empty states | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 378 | Loading states | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 379 | Error states | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 380 | Permission states | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 381 | Reduced motion | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 382 | Dynamic type | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 383 | VoiceOver labels | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 384 | Tap targets | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 385 | Contrast | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 386 | No repeated awkward titles | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 387 | No Android-ish UI patterns | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 388 | No glossy/glass cheap UI | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 389 | Screenshot/manual visual QA status | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | SwiftUI dark-first design system and screens exist with CTAs, source labels, and intentional empty states; manual visual/accessibility QA remains incomplete. | Whoordan/DesignSystem/*.swift; Whoordan/Features/**/*.swift; Whoordan/App/AppRootView.swift; WhoordanTests/DesignContractTests.swift; WhoordanUITests/WhoordanUITests.swift | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Outer AppRouter/AppEnvironment gate requires approval before protected screens, HealthKit, BLE, local mode, dashboard, haptics, journal, settings, or upload. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Premium CTAs or no-source language where implemented; some lower-priority surfaces still need richer empty states. | Not time-series sensitive or no dedicated stale policy yet. | DataSource and ConfidenceLevel labels exist; some screens still expose only summary-level labels. | DesignContractTests and WhoordanUITests launch/protected-screen skips. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |

### P. RELEASE / TESTING

| # | Feature | Status | User-facing behavior | Implementation details | Files/classes/functions inspected | Data sources used | Source priority | Approval-gate behavior | Privacy/consent behavior | Missing-data behavior | Stale-data behavior | Confidence/source-label behavior | Tests found | Validation performed | Physical validation status | Known limitations | Risks | Required next step |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 390 | Unit tests | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 391 | UI tests | IMPLEMENTED_NOT_PHYSICALLY_VALIDATED | Implemented in app code and simulator/unit validation, but not physically re-validated in this audit run. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Code built and unit/simulator tests passed; physical/manual flow not completed in this audit. | Physical proof not required or limited to app install/launch evidence. | Needs real iPhone/wearable/HealthKit manual validation and screenshots/log-safe evidence. | Main risk is overclaiming completeness beyond current implementation evidence. | Run scripted/manual validation on real iPhone or owned wearable and record redacted results. |
| 392 | Simulator build | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 393 | Simulator test | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 394 | Physical iPhone build/run | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical iPhone build, install, and launch succeeded on the configured physical iPhone via xcodebuild/devicectl. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 395 | Physical HealthKit validation | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 396 | Physical BLE/wearable validation | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 397 | Physical vibration validation | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Partially validated by build/static/unit checks; incomplete scope remains. | Not fully physically validated in this audit pass unless noted elsewhere; simulator/unit tests do not count as hardware proof. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 398 | TestFlight readiness | MISSING | No production user-facing workflow exists yet. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Missing state is the feature status; no production data path. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | No validation beyond absence/static inspection. | Physical proof not required or limited to app install/launch evidence. | No implementation currently found. | Main risk is overclaiming completeness beyond current implementation evidence. | Prioritize, design, implement, and test only if still in product scope. |
| 399 | App icon/assets | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 400 | Launch screen | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 401 | Bundle ID | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 402 | Signing/capabilities | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 403 | Privacy policy/terms | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 404 | Final legal copy | PARTIAL | Partially available; user can see some UI or behavior, but one or more required data, persistence, confidence, source, or validation pieces are incomplete. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Partially validated by build/static/unit checks; incomplete scope remains. | Physical proof not required or limited to app install/launch evidence. | Incomplete persistence, workflow depth, source labels, confidence, stale handling, tests, or manual validation. | Main risk is overclaiming completeness beyond current implementation evidence. | Finish missing data flow, persistence, UI labels, tests, and validation for this feature. |
| 405 | No raw private CSV committed | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 406 | No secrets committed | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 407 | No fake metrics | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |
| 408 | No unsafe medical claims | IMPLEMENTED_VALIDATED | Implemented and covered by current build/tests or deterministic inspection where physical hardware is not required. | Xcode project builds and tests; release readiness remains limited by physical validation, legal docs, TestFlight, and production persistence gaps. | Whoordan.xcodeproj; Whoordan/Resources/Assets.xcassets; Whoordan/Resources/Info.plist; Whoordan/Resources/Whoordan.entitlements; WhoordanTests/*; WhoordanUITests/*; validation command output from 2026-05-11 | App state, local cache, user session, and source-labeled health samples where applicable. | Not a measurement feature; app-owned state or explicit user action is source of truth. | Auth-only access is allowed before approval; protected features remain locked. | No service-role key, no medical claims, and protected data hidden before approval/revocation. | Unavailable/error states exist where relevant; not every workflow has complete copy. | Not time-series sensitive or no dedicated stale policy yet. | Not applicable or limited to state labels. | xcodebuild build/test, git diff --check, static safety search. | Validated by current xcodebuild/test/static/MCP evidence where applicable. | Physical proof not required or limited to app install/launch evidence. | Still needs regression coverage over time; validation is limited to inspected scope. | Main risk is overclaiming completeness beyond current implementation evidence. | Keep regression coverage; add physical/manual evidence where relevant. |

## Detailed Implementation Explanation

### 1. App Architecture
Whoordan uses the SwiftUI app lifecycle in `WhoordanApp`, a root `AppRootView`, `AppRouter`, and a single `AppEnvironment` observable object for app state and dependency wiring. The app modules are separated into `App`, `DesignSystem`, `Core`, `Features`, `Resources`, `WhoordanTests`, and `WhoordanUITests`. Dependencies are protocol-shaped for auth, approval, local storage, HealthKit, BLE, haptics, scoring, and health sync.

### 2. Approval/Auth System
Supabase password auth is implemented with REST calls and publishable-key configuration. Sessions are encoded into Keychain through `KeychainStore`. On launch and sign-in, `AppEnvironment` restores the session, fetches `public.user_access`, and routes only approved users into `MainTabView`. Pending, rejected, revoked, missing, unknown, or error states stay in `ApprovalLockedView`. On sign-out or non-approved refresh, BLE is stopped, haptics are cancelled, cached unlocked summary state is cleared, and protected state is reset. Approval does not rely on `user_metadata` or `raw_user_meta_data`.

### 3. Supabase/Cloud
The app derives Supabase URL from either explicit URL or project ID and accepts only publishable/anon-style public keys in app config. Health sync posts to `health_samples` using the signed-in user JWT and hashed `source_record_id`/`dedupe_key`. Upload is blocked unless the user is signed in, approved, and both cloud sync and health-data consent are enabled. A durable upload queue, retry/backoff, and repair scan now exist for local health samples. Conflict UI, account deletion, export workflows, and live two-user RLS probing remain incomplete. MCP inspection found RLS/forced RLS enabled on public tables and no metadata-based RLS policy, but no two-user RLS probe was run.

### 4. Local Storage
Auth secrets use Keychain. `FileProtectedLocalStore` now persists consent, summaries, health records, queues, and checkpoints to Application Support with iOS file protection; it is durable but not a full encrypted indexed health database. This is a production gap for large health datasets, local-only mode, offline retention, export, deletion, and checkpointing.

### 5. HealthKit
`HealthKitService` checks availability, requests read authorization, imports supported quantity/category/workout samples, and maps them into `HealthSample` with source labels, units, validation bounds, and confidence. Supported types include HR, RHR, HRV SDNN, respiratory rate, sleep analysis, steps, active energy, walking/running distance, oxygen saturation, body/wrist temperature, workouts, and VO2 max. Anchored incremental import and local checkpoint storage are now implemented. Background delivery registration uses `HKObserverQuery` for supported types. Apple Health write support is intentionally narrow and currently limited to supported user-created workout samples. Device launch/build passed, but real HealthKit import/background/write behavior was not manually re-validated in this pass.

### 6. BLE/Wearable
`WearableBLEService` wraps CoreBluetooth discovery, candidate sorting, auto-connect to connected/preferred devices, service/characteristic discovery, notification subscription, command writes, raw debug capture by explicit flag, and state updates. `WearableProtocol` implements UUID constants, 0xAA frame format, CRC8/CRC32, frame reassembly, command builders, init/realtime/haptic commands, metadata/event/firmware/data decoders, R10 HR extraction, R11 scaffold, R21 optical summary scaffold, safe source metadata, and stable dedupe IDs. Metadata batch markers now dispatch ACKs through the command characteristic, end-of-sync persists a BLE checkpoint, and disconnect/stop attempts realtime disable commands where safe. The app emits only plausible direct HR and diagnostic IMU summaries from wearable packets; it does not emit HRV, SpO2, steps, respiratory rate, calories, or sleep stages from unsupported raw packets. Physical wearable data flow was not re-tested in this pass.

### 7. Vibration/Haptics
`VibrationPattern` enforces repeat/duration/intensity limits. `VibrationPreviewService` requires approval, connected/historical/realtime wearable state, safe built-in patterns, and sends Harvard `0x4F` plus Maverick/Gen4 `0x13` commands with response requested. It reports approval required, not connected, unsupported, unsafe, started, failed, and terminated states. It does not fake actual vibration confirmation; physical haptic validation remains pending.

### 8. Scoring/Health Analytics
`WhoordanScoringService` implements original transparent recovery and strain estimates. Recovery uses available HRV, RHR, sleep, respiratory rate, and temperature inputs against simple baselines, with confidence based on contributor weight. Strain uses HR load where available and source-labeled movement/steps/energy. `HealthSourceResolver` enforces device-first source priority and blocks fake HRV from BPM-only data and uncalibrated SpO2. Rolling baselines, persisted personal normal ranges, full stress, strength load, cycle context, trend analytics, and full sleep planner logic are incomplete.

### 9. UI/UX
The SwiftUI app has a dark matte design system and main screens for Today, Recovery, Sleep, Heart, Device, Vibration, Journal, and Settings, plus settings sub-surfaces for Movement, Workouts, Strength, Body Signals, and Trends. The redesigned signal screens avoid duplicate giant titles and include missing-data CTAs. Visual, dynamic type, VoiceOver, tap target, contrast, and screenshot QA are only partially complete.

### 10. Testing/Validation
Current tests cover approval routing/guards, Supabase config and health sync consent, CSV privacy/sanitized fixtures, HealthKit mapping/source resolution/steps/dedupe, scoring, wearable protocol frames/commands/decoders, haptic command safety, design contracts, and one simulator UI launch. Physical app build/install/launch passed; HealthKit, BLE, wearable data, and physical haptics were not fully re-tested in this audit pass.

## Validation Results

| Command / check | Result | Notes |
|---|---|---|
| `git branch --show-current` | PASS | Branch was `swift-app`. |
| `git status --short` | PASS WITH DIRTY WORKTREE | SwiftUI migration branch has many expected deletes/adds/modified docs; no destructive revert was performed. |
| Static safety search | PASS | No client service-role key, committed raw private CSV, committed full raw BLE payload, production fake metric string, or user-metadata approval decision found in searched repo files. `.env` and private secret files were not read. |
| `xcodebuild -list -project Whoordan.xcodeproj` | PASS | Scheme `Whoordan`; targets `Whoordan`, `WhoordanTests`, `WhoordanUITests`. |
| `xcodebuild build -project Whoordan.xcodeproj -scheme Whoordan -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.4.1'` | PASS | Simulator build succeeded. |
| `xcodebuild test -project Whoordan.xcodeproj -scheme Whoordan -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.4.1'` | PASS | 71 unit tests passed. UI suite: 1 simulator launch test passed; 7 approved-session physical tests intentionally skipped. |
| `xcodebuild build -project Whoordan.xcodeproj -scheme Whoordan -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO` | PASS | Generic iOS no-codesign build succeeded with one non-blocking HealthKit deprecation warning for direct `HKWorkout` initializer use. |
| `xcrun xctrace list devices` | PASS | Showed the configured physical iPhone on iOS 26.4.1. |
| `xcrun devicectl list devices` | PASS WITH TOOL WARNING | Listed the iPhone as connected. `devicectl` also printed a CoreDevice provider warning, but install/launch still succeeded. |
| `xcodebuild build -project Whoordan.xcodeproj -scheme Whoordan -destination 'platform=iOS,id=<redacted-device-id>'` | PASS | Wireless physical iPhone build succeeded with Apple Development signing, HealthKit entitlement, and Supabase publishable config present in the built plist. |
| `xcrun devicectl device install app --device <redacted-device-id> .../Whoordan.app` | PASS | App installed on the connected physical iPhone. |
| `xcrun devicectl device process launch --device <redacted-device-id> com.w4rd2.whoordan` | PASS | App launched on the connected physical iPhone. |
| Supabase MCP RLS inspection | PASS WITH LIMITATION | All inspected public tables had RLS and forced RLS enabled; policies referenced `auth.uid()` and protected tables referenced `user_access`; no policy referenced user metadata. No live two-user RLS probe was run. |
| Supabase security advisors | WARN | Advisor reported leaked-password protection disabled. |
| Supabase performance advisors | INFO | Advisor reported unused indexes; not a P0/P1 app safety issue. |
| Physical HealthKit manual validation | NOT COMPLETED THIS PASS | App launched on device, but HealthKit permission/import/upload was not manually re-tested during this audit. |
| Physical BLE/wearable validation | NOT COMPLETED THIS PASS | App launched on device, but owned wearable connection/data flow was not manually re-tested during this audit. |
| Physical vibration validation | NOT COMPLETED THIS PASS | Haptic command building is tested; actual vibration was not re-tested during this audit. |

## Top 20 Highest-Priority Next Steps

1. Replace file-backed JSON storage with encrypted indexed SQLite/SwiftData-style storage and explicit deletion/export behavior.
2. Add a redacted physical HealthKit validation run on an approved account.
3. Add a redacted physical BLE validation run for the owned wearable, including connection state and decoded-safe-signal proof.
4. Complete Apple Health write queue draining for user-created workouts.
5. Add explicit battery/charging/wrist/temperature captures and tests when those real event packets are available.
6. Add full cloud conflict handling and multi-device repair UX.
7. Add live two-user RLS probe using safe test accounts.
8. Move HealthKit workout writes to `HKWorkoutBuilder` to remove the iOS 17 deprecation warning.
9. Add durable account-scoped local store partitioning and migration tests.
10. Add real background-delivery device validation for HealthKit observer callbacks.
11. Enable Supabase leaked-password protection in Dashboard.
12. Add account deletion and local/cloud data deletion workflows.
13. Add data export workflow that redacts secrets and respects health-data privacy.
14. Implement rolling baselines and personal normal ranges.
15. Expand source/confidence labels across all UI surfaces.
16. Finish Workouts history/detail and workout HR zone summaries.
17. Finish Sleep sessions, efficiency, debt/need, and trend persistence without fake stages.
18. Finish Journal/habit persistence and association-only insights with sample-size thresholds.
19. Perform manual accessibility QA for dynamic type, VoiceOver, contrast, and tap targets.
20. Run TestFlight/archive readiness after physical HealthKit/BLE/haptic validation is complete.

## What Must Be Tested On Real iPhone/Wearable

- Approved, pending, rejected, revoked, missing, and error approval flows with real Supabase sessions.
- HealthKit authorization, denied/partial/authorized states, per-type import, source labels, and no-upload-until-consent behavior.
- BLE permission prompt, auto-connect to the owned compatible wearable, service/characteristic discovery, init, historical sync, realtime data, safe HR extraction, diagnostics, malformed packet handling, and disconnect cleanup.
- Haptic preview command send and actual wearable vibration confirmation or honest unsupported state.
- Manual UI screenshots, dynamic type, VoiceOver basics, contrast, tap targets, and smooth scrolling.

## Intentionally Excluded / Not Copied

- AI coaching and AI chat are excluded.
- third-party formulas, proprietary scoring, UI trade dress, colors, wording, charts, and product claims are not copied.
- private wearable export values are not treated as formula truth; private exports are only allowed for schema, edge-case, aggregate benchmark, and sanitized fixture work.
- True HRV is not computed from BPM-only data.
- Medical-grade SpO2 is not produced from uncalibrated raw PPG.
- Steps are not fabricated from raw accelerometer data without a validated algorithm.
- Custom AFib, pregnancy detection, contraception, fertility, and diagnostic features are not implemented.
